name: Server 

on:
  push:
    branches:
      - "main"
    paths-ignore:
      - "client/**"

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: CI Pipeline 
        run: 'true'
