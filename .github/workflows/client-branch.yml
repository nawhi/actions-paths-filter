name: Client (Branch)

on:
  push:
    branches-ignore:
      - "main"
    paths-ignore:
      - "server/**"

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: CI Pipeline Here 
        run: 'true'
